# T9 R-34 live plan tooling

The enforced dispatcher implementation and its data file live outside this
product worktree at `/home/liamw/osl-plan/dispatch1.py` and
`/home/liamw/osl-plan/plan-b-gate.json`, by design. This commit records the
product-side RT-34 test that exercises that live authority.
