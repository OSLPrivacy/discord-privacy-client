[[permission]]
identifier = "allow-get-onboarding-preferences"
description = "Read the local, non-sensitive OSL Privacy preview preferences."
commands.allow = ["get_onboarding_preferences"]

[[permission]]
identifier = "allow-activate-native-manual-peer-context"
description = "Bind one verified friend scope to the exact current signed native Discord host generation; accepts no Discord credentials, process, window, or conversation identifiers."
commands.allow = ["activate_native_manual_peer_context"]

[[permission]]
identifier = "allow-activate-osl-chat-context"
description = "Allow only the trusted main OSL window to bind one verified friend to the fixed first-party OSL Chat service and account scope."
commands.allow = ["activate_osl_chat_context"]

[[permission]]
identifier = "allow-close-osl-chat-context"
description = "Allow only the trusted main OSL window to revoke the active fixed first-party OSL Chat lease."
commands.allow = ["close_osl_chat_context"]

[[permission]]
identifier = "allow-prepare-osl-chat-text"
description = "Allow only the trusted main OSL window to encrypt bounded first-party chat text for the active verified friend and deliver it through the authenticated OSL inbox."
commands.allow = ["prepare_osl_chat_text"]

[[permission]]
identifier = "allow-open-osl-chat-text"
description = "Allow only the trusted main OSL window to drain and open authenticated first-party chat messages for the active verified friend."
commands.allow = ["open_osl_chat_text"]

[[permission]]
identifier = "allow-list-osl-chat-history"
description = "Allow only the trusted main OSL window to read bounded encrypted-at-rest history for the currently active first-party OSL Chat."
commands.allow = ["list_osl_chat_history"]

[[permission]]
identifier = "allow-select-osl-chat-attachment"
description = "Allow only the trusted main OSL window to choose, encrypt, upload, and deliver one Pro attachment for the exact active verified OSL friend scope."
commands.allow = ["select_osl_chat_attachment"]

[[permission]]
identifier = "allow-list-osl-chat-attachments"
description = "Allow only the trusted main OSL window to list authenticated pending Pro attachments for the exact active verified OSL friend scope."
commands.allow = ["list_osl_chat_attachments"]

[[permission]]
identifier = "allow-open-osl-chat-attachment"
description = "Allow only the trusted main OSL window to authenticate and open one pending Pro attachment for the exact active verified OSL friend scope."
commands.allow = ["open_osl_chat_attachment"]

[[permission]]
identifier = "allow-set-native-discord-protected-overlay-open"
description = "From the trusted main OSL UI only, show or close the capture-resistant bundled OSL private layer over the exact current signed native Discord window."
commands.allow = ["set_native_discord_protected_overlay_open"]

[[permission]]
identifier = "allow-get-native-discord-overlay-state"
description = "Allow only the bundled native Discord overlay to read bounded non-secret protection state; the context token is never returned."
commands.allow = ["get_native_discord_overlay_state"]

[[permission]]
identifier = "allow-prepare-native-discord-overlay-text"
description = "Allow only the bundled native Discord overlay to encrypt bounded text for the active verified friend and deliver it through the authenticated OSL inbox; no context capability is accepted."
commands.allow = ["prepare_native_discord_overlay_text"]

[[permission]]
identifier = "allow-send-native-discord-overlay-carrier"
description = "Allow only the bundled native Discord overlay to place one fixed non-secret OSL carrier into the exact calibrated signed Discord composer after an explicit gesture; no text, HWND, PID, path, or timing trace is accepted."
commands.allow = ["send_native_discord_overlay_carrier"]

[[permission]]
identifier = "allow-open-native-discord-overlay-text"
description = "Allow only the bundled native Discord overlay to drain ordinary authenticated OSL relay notices and list view-once notices without revealing them for the exact active friend and native Discord lifecycle; no context capability is accepted."
commands.allow = ["open_native_discord_overlay_text"]

[[permission]]
identifier = "allow-reveal-native-discord-overlay-view-once"
description = "Allow only the bundled native Discord overlay to reveal one explicitly selected authenticated view-once message for the exact active friend and native Discord lifecycle; only its opaque message id is accepted."
commands.allow = ["reveal_native_discord_overlay_view_once"]

[[permission]]
identifier = "allow-burn-native-discord-overlay-chat"
description = "Allow only the bundled native Discord overlay, after its own explicit two-step confirmation, to burn the exact Rust-held OSL friend scope and local protected ledger. It cannot touch Discord history, profiles, logins, or recipient copies."
commands.allow = ["burn_native_discord_overlay_chat"]

[[permission]]
identifier = "allow-set-native-discord-overlay-security"
description = "Allow only the bundled native Discord overlay to change the existing bounded TTL and decrypted-display preference for its exact Rust-held friend context; no context capability is accepted."
commands.allow = ["set_native_discord_overlay_security"]

[[permission]]
identifier = "allow-set-hub-screenshot-protection"
description = "Apply or remove Windows capture resistance for the trusted OSL Privacy window and its child service surface."
commands.allow = ["set_hub_screenshot_protection"]

[[permission]]
identifier = "allow-copy-hub-friend-invite"
description = "Copy only the freshly signed friend invite obtained from the active unlocked Rust identity to the Windows clipboard; accepts no content and cannot read the clipboard."
commands.allow = ["copy_hub_friend_invite"]

[[permission]]
identifier = "allow-list-hub-app-notifications"
description = "List bounded local OSL security notifications after explicit opt-in; never reads hosted page content."
commands.allow = ["list_hub_app_notifications"]

[[permission]]
identifier = "allow-set-hub-notifications-enabled"
description = "Enable or disable local OSL security-notification reads for this running OSL Privacy session."
commands.allow = ["set_hub_notifications_enabled"]

[[permission]]
identifier = "allow-save-onboarding-preferences"
description = "Save the local, non-sensitive OSL Privacy preview preferences."
commands.allow = ["save_onboarding_preferences"]

[[permission]]
identifier = "allow-scan-local-privacy"
description = "Scan bounded caller-provided visible message text in local process memory without persistence or network access."
commands.allow = ["scan_local_privacy"]

[[permission]]
identifier = "allow-initialize-scrub-index"
description = "Initialize one bounded encrypted local-only Scrub index for explicitly selected profiles owned by the active OSL identity."
commands.allow = ["initialize_scrub_index"]

[[permission]]
identifier = "allow-append-scrub-index-chunk"
description = "Append one bounded caller-provided export or OSL-visible-data chunk to the authenticated local Scrub index; provides no discovery, network, or deletion authority."
commands.allow = ["append_scrub_index_chunk"]

[[permission]]
identifier = "allow-get-scrub-index-status"
description = "Read bounded progress metadata for the active identity's encrypted local Scrub index."
commands.allow = ["get_scrub_index_status"]

[[permission]]
identifier = "allow-pause-scrub-index"
description = "Pause the exact active local Scrub import by its random identifier."
commands.allow = ["pause_scrub_index"]

[[permission]]
identifier = "allow-resume-scrub-index"
description = "Resume the exact active local Scrub import by its random identifier."
commands.allow = ["resume_scrub_index"]

[[permission]]
identifier = "allow-cancel-scrub-index"
description = "Cancel and remove only the active identity's fixed encrypted Scrub index directory; cannot delete service messages."
commands.allow = ["cancel_scrub_index"]

[[permission]]
identifier = "allow-list-linked-services"
description = "Read the static demo service and account registry."
commands.allow = ["list_linked_services"]

[[permission]]
identifier = "allow-create-service-account"
description = "Create local metadata for one fresh isolated service profile; stores no credentials or cookies."
commands.allow = ["create_service_account"]

[[permission]]
identifier = "allow-remove-service-account"
description = "Close and atomically detach one exact isolated service profile, then remove its local registry record."
commands.allow = ["remove_service_account"]

[[permission]]
identifier = "allow-get-core-readiness"
description = "Read non-sensitive readiness for the locally linked original OSL core."
commands.allow = ["get_core_readiness"]

[[permission]]
identifier = "allow-list-core-features"
description = "List the static original OSL feature integration manifest."
commands.allow = ["list_core_features"]

[[permission]]
identifier = "allow-get-hub-license-state"
description = "Read bounded local Free or Pro activation state from the trusted bundled OSL Privacy UI."
commands.allow = ["get_hub_license_state"]

[[permission]]
identifier = "allow-get-mass-cleanup-capabilities"
description = "After a native Pro check, list only static fail-closed Mass Cleanup adapter metadata; no service data is read."
commands.allow = ["get_mass_cleanup_capabilities"]

[[permission]]
identifier = "allow-discover-mass-cleanup-targets"
description = "Pro-gated Mass Cleanup discovery seam; it rejects every request until a reviewed local semantic adapter is installed."
commands.allow = ["discover_mass_cleanup_targets"]

[[permission]]
identifier = "allow-execute-mass-cleanup-batch"
description = "Pro-gated Mass Cleanup execution seam; it has no mutation adapter and always fails closed in this build."
commands.allow = ["execute_mass_cleanup_batch"]

[[permission]]
identifier = "allow-validate-hub-activation-code"
description = "Validate and seal one browser-delivered activation code from the trusted bundled OSL Privacy UI."
commands.allow = ["validate_hub_activation_code"]

[[permission]]
identifier = "allow-clear-hub-activation-code"
description = "Remove the locally sealed activation code from the trusted bundled OSL Privacy UI."
commands.allow = ["clear_hub_activation_code"]

[[permission]]
identifier = "allow-unlock-hub-password-gate"
description = "Verify the original constant-time main, stealth, and burn password gate and execute exactly the role's trusted local consequence."
commands.allow = ["unlock_hub_password_gate"]

[[permission]]
identifier = "allow-create-hub-osl-identity"
description = "Create one service-neutral OSL identity in persistent device storage from the trusted bundled OSL Privacy UI."
commands.allow = ["create_hub_osl_identity"]

[[permission]]
identifier = "allow-import-hub-osl-identity-phrase"
description = "Import one OSL-native identity phrase into an empty isolated OSL Privacy account from the trusted bundled OSL Privacy UI."
commands.allow = ["import_hub_osl_identity_phrase"]

[[permission]]
identifier = "allow-setup-hub-main-password"
description = "Set a strong local main passphrase once for an already-loaded isolated OSL Privacy identity and reload encrypted state."
commands.allow = ["setup_hub_main_password"]

[[permission]]
identifier = "allow-get-hub-password-role-status"
description = "Read whether trusted-local stealth and burn password roles are configured without returning hashes or marker contents."
commands.allow = ["get_hub_password_role_status"]

[[permission]]
identifier = "allow-set-hub-stealth-password"
description = "Configure the original OSL stealth password after ordinary-main-password verification in trusted local UI."
commands.allow = ["set_hub_stealth_password"]

[[permission]]
identifier = "allow-remove-hub-stealth-password"
description = "Remove the original OSL stealth password after ordinary-main-password verification in trusted local UI."
commands.allow = ["remove_hub_stealth_password"]

[[permission]]
identifier = "allow-set-hub-burn-password"
description = "Configure the original OSL burn password after ordinary-main-password verification in trusted local UI; this command does not execute a burn."
commands.allow = ["set_hub_burn_password"]

[[permission]]
identifier = "allow-remove-hub-burn-password"
description = "Remove the original OSL burn password after ordinary-main-password verification in trusted local UI."
commands.allow = ["remove_hub_burn_password"]

[[permission]]
identifier = "allow-check-hub-for-updates"
description = "Check the compiled-in signed OSL Privacy update endpoint and return bounded plain metadata."
commands.allow = ["check_hub_for_updates"]

[[permission]]
identifier = "allow-install-hub-update"
description = "After explicit trusted-UI consent, download, signature-verify, install, and restart an OSL Privacy update."
commands.allow = ["install_hub_update"]

[[permission]]
identifier = "allow-open-hub-releases-page"
description = "Open the single compiled-in OSL GitHub releases page in the system browser."
commands.allow = ["open_hub_releases_page"]

[[permission]]
identifier = "allow-list-native-apps"
description = "Report installed status for the four fixed Windows-native messaging applications without exposing executable paths."
commands.allow = ["list_native_apps"]

[[permission]]
identifier = "allow-install-native-app"
description = "Start one fixed silent winget or Microsoft Store background install after an explicit trusted-UI click, without shell input or automatic elevation."
commands.allow = ["install_native_app"]

[[permission]]
identifier = "allow-get-mullvad-status"
description = "Report only whether Mullvad exists in one of two fixed standard Windows locations; never reads VPN account, tunnel, traffic, or configuration state."
commands.allow = ["get_mullvad_status"]

[[permission]]
identifier = "allow-install-mullvad"
description = "Start only the fixed MullvadVPN.MullvadVPN package through winget after an explicit trusted-UI click, without caller-supplied package, path, or arguments."
commands.allow = ["install_mullvad"]

[[permission]]
identifier = "allow-open-mullvad"
description = "Open only a fixed detected Mullvad desktop executable with no arguments after an explicit trusted-UI click."
commands.allow = ["open_mullvad"]

[[permission]]
identifier = "allow-host-mullvad-window"
description = "After explicit consent, borrow only the one eligible existing Mullvad window from the current Windows session; no path, process, window, account, or VPN state is accepted or returned."
commands.allow = ["host_mullvad_window"]

[[permission]]
identifier = "allow-resize-mullvad-window"
description = "Realign only the already verified borrowed Mullvad window to OSL's content bounds."
commands.allow = ["resize_mullvad_window"]

[[permission]]
identifier = "allow-focus-mullvad-window"
description = "Focus only the already verified borrowed Mullvad window."
commands.allow = ["focus_mullvad_window"]

[[permission]]
identifier = "allow-restore-mullvad-window"
description = "Restore the borrowed Mullvad window's original owner, styles, and bounds without closing Mullvad."
commands.allow = ["restore_mullvad_window"]

[[permission]]
identifier = "allow-list-browser-imports"
description = "Report only whether six fixed supported browsers exist in standard Windows install locations; never reads browser profiles, cookies, or credentials."
commands.allow = ["list_browser_imports"]

[[permission]]
identifier = "allow-open-browser-import"
description = "After an explicit click, open one detected browser's fixed password-manager/export surface; the browser retains confirmation and OSL never reads the database or exported CSV."
commands.allow = ["open_browser_import"]

[[permission]]
identifier = "allow-get-firefox-status"
description = "Report only whether Firefox is installed in a fixed Windows location or can be installed through the verified Microsoft App Installer package."
commands.allow = ["get_firefox_status"]

[[permission]]
identifier = "allow-install-firefox"
description = "After explicit local consent, start only the fixed Mozilla.Firefox package through the verified Microsoft App Installer executable."
commands.allow = ["install_firefox"]

[[permission]]
identifier = "allow-begin-browser-account-import"
description = "After explicit local consent, open Firefox's fixed migration wizard in OSL's isolated Firefox profile; browser detection, profile path, executable, and arguments are native fixed values."
commands.allow = ["begin_browser_account_import"]

[[permission]]
identifier = "allow-begin-protected-browser-import"
description = "After explicit local consent, validate a bounded list of detected browser IDs and open Firefox's fixed migration wizard in the active OSL identity's isolated profile; no credential, path, URL, or arbitrary argument crosses IPC."
commands.allow = ["begin_protected_browser_import"]

[[permission]]
identifier = "allow-finish-protected-browser-import"
description = "Close only the exact retained OSL-owned Firefox migration process after a queued import finishes or is skipped."
commands.allow = ["finish_protected_browser_import"]

[[permission]]
identifier = "allow-launch-firefox-service"
description = "Open one reviewed service origin in the active identity's OSL-owned Firefox profile; callers cannot provide a URL, path, or browser argument."
commands.allow = ["launch_firefox_service"]

[[permission]]
identifier = "allow-get-default-browser-companion-status"
description = "Report only whether Windows' current HTTPS default resolves to one fixed Authenticode-verified supported browser."
commands.allow = ["get_default_browser_companion_status"]

[[permission]]
identifier = "allow-host-default-browser-companion"
description = "After explicit consent, open one fixed service origin in a new app-style window of the verified Windows default browser using its normal profile; no URL, path, profile, policy, or hook is accepted."
commands.allow = ["host_default_browser_companion"]

[[permission]]
identifier = "allow-resize-default-browser-companion"
description = "Realign only the exact default-browser app window created and retained by OSL."
commands.allow = ["resize_default_browser_companion"]

[[permission]]
identifier = "allow-focus-default-browser-companion"
description = "Focus only the exact default-browser app window created and retained by OSL."
commands.allow = ["focus_default_browser_companion"]

[[permission]]
identifier = "allow-detach-default-browser-companion"
description = "Restore and hide only the exact default-browser app window created and retained by OSL."
commands.allow = ["detach_default_browser_companion"]

[[permission]]
identifier = "allow-host-native-app-window"
description = "Experimentally launch only a verified isolated secondary instance of one fixed messaging app and align its owned borderless window inside OSL; unsupported clients fail closed."
commands.allow = ["host_native_app_window"]

[[permission]]
identifier = "allow-resize-native-app-window"
description = "Realign only the exact OSL-spawned native client window to the trusted OSL content bounds."
commands.allow = ["resize_native_app_window"]

[[permission]]
identifier = "allow-focus-native-app-window"
description = "Focus only the exact OSL-spawned native client window currently owned by OSL."
commands.allow = ["focus_native_app_window"]

[[permission]]
identifier = "allow-detach-native-app-window"
description = "Restore ownership, styles, and bounds only for the exact OSL-spawned native client window."
commands.allow = ["detach_native_app_window"]

[[permission]]
identifier = "allow-open-service-host"
description = "Open one explicitly allowlisted service in an isolated per-account child webview."
commands.allow = ["open_service_host"]

[[permission]]
identifier = "allow-close-service-host"
description = "Close the currently hosted service child webview."
commands.allow = ["close_service_host"]

[[permission]]
identifier = "allow-set-local-protected-sheet-open"
description = "Resize only the exact active capability-free service host to reveal or reclaim the fixed trusted local-protection sheet area."
commands.allow = ["set_local_protected_sheet_open"]

[[permission]]
identifier = "allow-activate-local-loopback-context"
description = "Create a local-only self-recipient context from the unlocked owner, one owned active service account, its current host generation, and a bounded opaque conversation id."
commands.allow = ["activate_local_loopback_context"]

[[permission]]
identifier = "allow-activate-manual-peer-context"
description = "Activate one renderer-selected existing friend for the exact active owned service account; recipient keys and the symmetric manual-DM binding are derived locally without inspecting the remote service page."
commands.allow = ["activate_manual_peer_context"]

[[permission]]
identifier = "allow-prepare-peer-prose-text"
description = "Prepare marker-free encrypted copy text for the exact active manual peer context without placing or sending it through the hosted service."
commands.allow = ["prepare_peer_prose_text"]

[[permission]]
identifier = "allow-open-peer-prose-text"
description = "Open one bounded manually pasted peer ciphertext in the trusted local UI for the exact active manual peer context."
commands.allow = ["open_peer_prose_text"]

[[permission]]
identifier = "allow-place-prepared-capsule"
description = "After an explicit trusted-UI action, focus the exact active service surface and place a prepared capsule without sending it."
commands.allow = ["place_prepared_capsule"]

[[permission]]
identifier = "allow-prepare-encrypted-text"
description = "Prepare original-OSL encrypted capsules for the exact active trusted context."
commands.allow = ["prepare_encrypted_text"]

[[permission]]
identifier = "allow-decrypt-hub-capsule"
description = "Decrypt one bounded capsule into the trusted local overlay for the exact active context."
commands.allow = ["decrypt_hub_capsule"]

[[permission]]
identifier = "allow-decrypt-local-protected-capsule"
description = "Decrypt one locally-ledgered self-encrypted capsule in its exact active context."
commands.allow = ["decrypt_local_protected_capsule"]

[[permission]]
identifier = "allow-prepare-local-protected-text-with-policy"
description = "Prepare one self-encrypted context-bound message with an optional locally enforced one-open policy; does not claim peer E2EE or screenshot prevention."
commands.allow = ["prepare_local_protected_text_with_policy"]

[[permission]]
identifier = "allow-prepare-hub-attachment"
description = "Seal one bounded attachment with the original OSL core for the exact active trusted context; does not upload or send it."
commands.allow = ["prepare_hub_attachment"]

[[permission]]
identifier = "allow-open-hub-attachment"
description = "Open one bounded OSL attachment only in the exact active trusted context and return plaintext solely to the bundled local UI."
commands.allow = ["open_hub_attachment"]

[[permission]]
identifier = "allow-select-native-discord-overlay-attachment"
description = "Allow only the bundled native Discord overlay to open a system picker and stream one supported selected file without exposing its path or bytes."
commands.allow = ["select_native_discord_overlay_attachment"]

[[permission]]
identifier = "allow-list-native-discord-overlay-attachments"
description = "List authenticated pending attachment display metadata for only the active native Discord friend scope; never return capabilities, keys, paths, or bytes."
commands.allow = ["list_native_discord_overlay_attachments"]

[[permission]]
identifier = "allow-open-native-discord-overlay-attachment"
description = "Stream, authenticate, decrypt, and open one opaque attachment ID in a native viewer for only the active native Discord friend scope."
commands.allow = ["open_native_discord_overlay_attachment"]

[[permission]]
identifier = "allow-export-hub-friend-code"
description = "Export the active identity's signed public friend code and safety number."
commands.allow = ["export_hub_friend_code"]

[[permission]]
identifier = "allow-add-hub-friend"
description = "Validate and add one signed OSL friend code to encrypted local People state."
commands.allow = ["add_hub_friend"]

[[permission]]
identifier = "allow-verify-hub-friend-safety-number"
description = "Record explicit out-of-band verification of one friend's safety number."
commands.allow = ["verify_hub_friend_safety_number"]

[[permission]]
identifier = "allow-list-hub-people"
description = "List bounded People metadata from encrypted local state."
commands.allow = ["list_hub_people"]

[[permission]]
identifier = "allow-set-hub-friend-nickname"
description = "Set or clear a bounded nickname in encrypted device-local People state."
commands.allow = ["set_hub_friend_nickname"]

[[permission]]
identifier = "allow-set-active-hub-friend-permission"
description = "Grant or revoke one verified person's permission for only the exact active hosted context."
commands.allow = ["set_active_hub_friend_permission"]

[[permission]]
identifier = "allow-get-active-hub-context-security"
description = "Read TTL and decrypt-display settings for only the exact active hosted context."
commands.allow = ["get_active_hub_context_security"]

[[permission]]
identifier = "allow-set-active-hub-context-security"
description = "Set TTL and decrypt-display settings for only the exact active hosted context."
commands.allow = ["set_active_hub_context_security"]

[[permission]]
identifier = "allow-list-hub-identities"
description = "List bounded metadata for unlocked OSL Privacy identity slots."
commands.allow = ["list_hub_identities"]

[[permission]]
identifier = "allow-create-hub-identity-slot"
description = "Create an additional encrypted OSL Privacy identity slot from trusted local UI."
commands.allow = ["create_hub_identity_slot"]

[[permission]]
identifier = "allow-recover-hub-identity-slot"
description = "Recover an additional OSL Privacy identity into a new encrypted slot from trusted local UI."
commands.allow = ["recover_hub_identity_slot"]

[[permission]]
identifier = "allow-switch-hub-identity"
description = "Close hosted services, invalidate broker context, and switch the active encrypted OSL Privacy identity slot."
commands.allow = ["switch_hub_identity"]

[[permission]]
identifier = "allow-burn-active-hub-identity"
description = "Permanently detach the active OSL Privacy identity and report local and remote cleanup outcomes separately."
commands.allow = ["burn_active_hub_identity"]

[[permission]]
identifier = "allow-execute-hub-full-cleanup"
description = "After explicit trusted confirmation, close hosts and delete every fixed OSL Privacy artifact without touching original Discord OSL data."
commands.allow = ["execute_hub_full_cleanup"]

[[permission]]
identifier = "allow-get-hub-service-burn-readiness"
description = "Preview the immutable, complete locally indexed OSL scope manifest for one owned service account without touching its login profile or native history."
commands.allow = ["get_hub_service_burn_readiness"]

[[permission]]
identifier = "allow-burn-hub-service-account"
description = "After exact manifest confirmation, burn only locally indexed OSL protected state for one owned service account while preserving its login profile, cookies, and native history."
commands.allow = ["burn_hub_service_account"]

[[permission]]
identifier = "allow-burn-active-hub-context"
description = "Burn only the exact active trusted service/account context and report local versus remote outcomes separately."
commands.allow = ["burn_active_hub_context"]
