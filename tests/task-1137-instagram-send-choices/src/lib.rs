#[path = "../../../apps/osl-hub/src/instagram_send.rs"]
mod instagram_send;

// Compile the preparation boundary beside both of its required website gates.
// The focused filter below runs only TASK 1137, but Rust still type-checks the
// shared placement/read-back and private-composer implementations it builds on.
#[path = "../../../apps/osl-hub/src/website_driver.rs"]
mod website_driver;
