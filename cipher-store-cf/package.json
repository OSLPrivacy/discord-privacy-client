{
  "name": "oslprivacy-cipher-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OSL cipher-store on Cloudflare Workers + D1. Receives short-TTL ciphertext blobs and serves them back by random 8-byte ID. No identity logging, no IP retention. Subpoena-resistant by design — see DEPLOY.md.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply osl-cipher-store-prod --local",
    "db:migrate:prod": "wrangler d1 migrations apply osl-cipher-store-prod --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
