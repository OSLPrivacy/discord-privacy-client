// Scaffolding placeholder for the Layer 11 encryption-overlay UI.
// The main Tauri window navigates directly to https://discord.com/app
// via `windows[0].url` in tauri.conf.json. No window loads this bundle
// at runtime yet. `tsc --noEmit` and `vitest run` both need at least
// one input file so CI typechecks and runs against a real module.

export const WEBVIEW_SCAFFOLDING_VERSION = "0.0.1";
