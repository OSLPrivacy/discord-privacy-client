# TASK 1089d — WhatsApp Desktop Status place

Research date: 2026-08-10 (America/Los_Angeles).

The Windows WhatsApp Desktop Status surface is named `Status`. Its visible
entry control is `My status`, with the action text `Click to add status update`.
Current desktop reporting names the resulting composer `Create a status`.

The configured QA VM could not be opened in this run: Azure rejected the
session-discovery command because subscription
`8d63ea78-a90a-45cb-92bb-6bde80a72360` is `ReadOnlyDisabledSubscription`.
The request therefore stopped before UI input and no Status was posted.

The dated source screen capture and full command log are retained outside the
worktree at `/home/liamw/osl-plan/OSL-AUDITS/evidence/1089d.md` and
`1089d-whatsapp-status-2026-08-10.jpeg` alongside it.
