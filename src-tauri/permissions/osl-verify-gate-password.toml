"$schema" = "../gen/schemas/desktop-schema.json"

# Phase 7d-B2/B3: 3-way password verification at the boot gate.
[[permission]]
identifier = "allow-osl-verify-gate-password"
description = "Permits the password_gate origin to invoke `osl_verify_gate_password` (constant-time compare across main + stealth + burn hashes; returns role string + shared lockout counter)."
commands.allow = ["osl_verify_gate_password"]
