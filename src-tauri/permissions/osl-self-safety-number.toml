"$schema" = "../gen/schemas/desktop-schema.json"

# REGISTER-FIX: open signed registration + TOFU key-change surface.
[[permission]]
identifier = "allow-osl-self-safety-number"
description = "Permits invoking `osl_self_safety_number` (derive our own identity safety number for OOB verification)."
commands.allow = ["osl_self_safety_number"]
