"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-osl-create-friend-request"
description = "Permits the main webview to invoke `osl_create_friend_request` and persist a pending friend request record."
commands.allow = ["osl_create_friend_request"]
