"$schema" = "../gen/schemas/desktop-schema.json"

# Permission for Phase 7b whitelist-invitation construction.
[[permission]]
identifier = "allow-osl-send-whitelist-invitation"
description = "Permits the discord.com main webview to invoke `osl_send_whitelist_invitation` (build the wire-format type=0x02 invitation for a peer + scope). Phase 7b — see §7.1."
commands.allow = ["osl_send_whitelist_invitation"]
