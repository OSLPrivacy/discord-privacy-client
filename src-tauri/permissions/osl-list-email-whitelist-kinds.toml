"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-osl-list-email-whitelist-kinds"
description = "Permits the trusted local settings window to invoke `osl_list_email_whitelist_kinds` (static list of supported email whitelist rule kinds)."
commands.allow = ["osl_list_email_whitelist_kinds"]
