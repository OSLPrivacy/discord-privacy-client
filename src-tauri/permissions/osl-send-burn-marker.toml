"$schema" = "../gen/schemas/desktop-schema.json"

# Permission for Phase 7b burn-marker construction.
[[permission]]
identifier = "allow-osl-send-burn-marker"
description = "Permits the discord.com main webview to invoke `osl_send_burn_marker` (build the wire-format type=0x01 burn marker for a scope). Phase 7b — see §3."
commands.allow = ["osl_send_burn_marker"]
