"$schema" = "../gen/schemas/desktop-schema.json"

# Phase 7d-B2: is a stealth password configured?
[[permission]]
identifier = "allow-osl-stealth-password-status"
description = "Permits trusted local UI surfaces to invoke `osl_stealth_password_status` (returns { is_set: bool }). Phase 7d-B2."
commands.allow = ["osl_stealth_password_status"]
