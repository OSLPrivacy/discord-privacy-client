"$schema" = "../gen/schemas/desktop-schema.json"

# Permission for Phase 7b whitelist-response construction.
[[permission]]
identifier = "allow-osl-send-whitelist-response"
description = "Permits the discord.com main webview to invoke `osl_send_whitelist_response` (build the wire-format type=0x03 accept/decline response). Phase 7b — see §§7.3, 7.4."
commands.allow = ["osl_send_whitelist_response"]
