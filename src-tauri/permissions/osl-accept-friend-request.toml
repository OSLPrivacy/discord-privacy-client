"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-osl-accept-friend-request"
description = "Permits the main webview to invoke `osl_accept_friend_request` and move a saved pending friend request to accepted."
commands.allow = ["osl_accept_friend_request"]
