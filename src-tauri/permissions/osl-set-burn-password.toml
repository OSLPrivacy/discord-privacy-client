"$schema" = "../gen/schemas/desktop-schema.json"

# Phase 7d-B3: set/rotate burn password (destructive trigger).
[[permission]]
identifier = "allow-osl-set-burn-password"
description = "Permits trusted local UI surfaces to invoke `osl_set_burn_password` (verify main, write burn_password_hash to marker). Uniqueness check against main + stealth. Phase 7d-B3."
commands.allow = ["osl_set_burn_password"]
