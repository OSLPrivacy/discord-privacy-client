"$schema" = "../gen/schemas/desktop-schema.json"

# Phase 7d-B3: is a burn password configured?
[[permission]]
identifier = "allow-osl-burn-password-status"
description = "Permits the discord.com main webview to invoke `osl_burn_password_status` (returns { is_set: bool }). Phase 7d-B3."
commands.allow = ["osl_burn_password_status"]
