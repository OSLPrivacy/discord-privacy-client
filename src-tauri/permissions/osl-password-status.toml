"$schema" = "../gen/schemas/desktop-schema.json"

# Phase 7d-B1: is a main password set?
[[permission]]
identifier = "allow-osl-password-status"
description = "Permits the discord.com main webview AND the local password_gate origin to invoke `osl_password_status` (read-only — returns { is_set: bool }, presence of password_marker.json). Phase 7d-B1."
commands.allow = ["osl_password_status"]
