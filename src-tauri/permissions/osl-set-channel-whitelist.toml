"$schema" = "../gen/schemas/desktop-schema.json"

# W2: server/channel whitelist + membership feed.
[[permission]]
identifier = "allow-osl-set-channel-whitelist"
description = "Permits invoking `osl_set_channel_whitelist` (W2 server/channel whitelist + dynamic membership)."
commands.allow = ["osl_set_channel_whitelist"]
