# TASK 3684b evidence — prove the one-megabit send check can fail

Canonical audit copy: `/home/liamw/osl-plan/OSL-AUDITS/evidence/3684b.md`.

## Break-it result

In an archive copy at `/tmp/osl-3684b.cxwDRI`, only the request-body reader's
Cancel guard was disabled:

```diff
-        if self.progress.as_ref().is_some_and(ProChunkedUploadProgress::is_cancelled) {
+        if self.progress.as_ref().is_some_and(|_| false) {
```

Focused command:

```text
CARGO_TARGET_DIR=/mnt/d/osl-lane-targets/b cargo test -p ipc --test task_3684_one_mbit_protected_send -- --test-threads=1 --nocapture
```

The mutant produced:

```text
assertion `left == right` failed: active-upload count stays 1 after Cancel while upload pieces continue
  left: 1
 right: 0
test result: FAILED. 0 passed; 1 failed; finished in 215.86s
MUTANT_EXIT_CODE=101
```

The copy was deleted and an existence check printed
`THROWAWAY_COPY_EXISTS=0`.

## Honest result

The same focused command against `/home/liamw/osl-exec-b` produced:

```text
TASK3684 file_bytes=26214400 measured_speed_mbit=0.999998 progress_rises=16 interrupted=true retry_completed=true original_sha256=900b5eaeb72ceb01a004bb8b5e054e68d3c681230aa3dc6ab50487252affeac7 received_sha256=900b5eaeb72ceb01a004bb8b5e054e68d3c681230aa3dc6ab50487252affeac7 received_message_count=0->1 active_upload_count=1->0
test result: ok. 1 passed; 0 failed; finished in 215.99s
REAL_EXIT_CODE=0
```

Before the copy, after deleting it, and after the honest run, the real source
and existing dirty state had identical fingerprints:

```text
HEAD=3e26d96b9d19242be8311b5daa358d8c903041ff
cipher_store_client.rs=dca0d680f7bd73df077d3ec1318e99a2c2a020a9e55b1708bc86ebbdb85582ce
task_3684_one_mbit_protected_send.rs=e22587218c8f9c7e1d2fc5d36a1626b0a45e682afd8e1cc2775c91c560a66e9a
git_diff_binary_fingerprint=917948558b5b42e2072afd4898a3c81435a57a06ef34807454b8ef5daa652f51
git_status_fingerprint=007bf4eb01a835354072b73b97f659a23d67309d7c9e0dfebc56705c168d749e
```

## Finish line

- [x] Red: exit `101`, active-upload actual count `1` after Cancel.
- [x] Copy discarded: `THROWAWAY_COPY_EXISTS=0`.
- [x] Real gate green: exit `0`, `1 passed; 0 failed`, count `1->0`.
- [x] Real code unchanged: all before/between/after fingerprints matched.
