# TASK 5070b — prove the capture notices absence

## Commands run

From `/home/liamw/osl-exec-b`:

```text
node -e "const fs=require('fs'); const s=fs.readFileSync('apps/osl-hub-ui/src/main.ts','utf8'); console.log((s.match(/export type Route = ([^;]+)/)||[])[1]||'missing')"
test -f apps/osl-hub-ui/screenshots/capture-all-routes.mjs && echo present || echo 'MISSING capture-all-routes.mjs'
rg -n 'osl-chat-profile-appearance|osl-chat-start-something|osl-chat-safety-number' apps/osl-hub-ui/src apps/osl-hub-ui/screenshots || true
git show 35e12733f:apps/osl-hub-ui/screenshots/capture-all-routes.mjs >/dev/null && echo 'historical harness present'
```

Printed output:

```text
"onboarding" | "home" | "inbox" | "people" | "privacy" | "activity" | "connections" | "service" | "settings" | "mullvad" | "osl-chat" | "osl-mail" | "osl-servers" | "signal-qa"
MISSING capture-all-routes.mjs
historical harness present
```

The current lane has no `capture-all-routes.mjs` and no profile-window trigger
names. The only copy is in historical commit `35e12733f`; using it would not
be a run against the current gate-5070 build. Therefore the requested disabled
trigger run, PNG byte delta, honest recapture, and plain-thread negative case
cannot be produced from this lane without importing another lane's work.

The available screenshot test command was also run:

```text
CARGO_TARGET_DIR=/mnt/d/osl-lane-targets/b npm run test:node -- --test-name-pattern='capture|PNG|screen'
```

It produced passing legacy capture checks, but unrelated existing failures in
boot timing, no-recovery-secret, Create password, and cover insertion. It does
not contain a TASK 5070 check.

## Finish line

- [ ] Disabled-trigger run names the profile screen as failed — impossible: harness absent.
- [ ] Disabled-trigger run leaves 0 new PNG bytes — impossible: harness absent.
- [ ] Honest build recaptures it as a non-blank image — impossible: route absent.
- [ ] Plain-thread PNG under popup name fails — impossible: gate check absent.

Status: blocked by missing gate-5070 harness and routes in this lane; no
workaround or fabricated capture evidence was used.
