# TASK 5084b — prove the carve moved and did not rewrite

All commands were run from the isolated lane or the explicitly named temporary
copies. No sibling worktree was changed.

## Before uidiff table

Command:

```text
cd /home/liamw/osl-plan/OSL-AUDITS
python3 uidiff.py
```

Exit: `0`. The seven carved-screen rows printed:

| screen | raw% | structure% |
|---|---:|---:|
| osl-chat-settings-popup | 6.6 | 13.4 |
| settings-appearance | 4.9 | 7.7 |
| settings-whitelisting | 4.8 | 7.4 |
| settings-scrub | 3.0 | 3.2 |
| settings-account | 4.4 | 5.3 |
| home | 4.6 | 4.8 |
| settings-privacy | 3.9 | 3.6 |

The same run printed `mean structure: 3.3%`, `worst: osl-chat-settings-popup
at 13.4%`, and `paired captures: 42/45`.

## After uidiff table

Command: the same `python3 uidiff.py`, after building a copy archived from
carve commit `1eefd829f` (`TASK 5084 carve renderers out of main.ts`).

Exit: `0`. The seven rows were exactly:

| screen | raw% | structure% |
|---|---:|---:|
| osl-chat-settings-popup | 6.6 | 13.4 |
| settings-appearance | 4.9 | 7.7 |
| settings-whitelisting | 4.8 | 7.4 |
| settings-scrub | 3.0 | 3.2 |
| settings-account | 4.4 | 5.3 |
| home | 4.6 | 4.8 |
| settings-privacy | 3.9 | 3.6 |

The aggregate again printed `mean structure: 3.3%`, `worst:
osl-chat-settings-popup at 13.4%`, and `paired captures: 42/45`. Every listed
number matches to one decimal.

## Build and break-it check

The after-carve copy was created with `git archive 1eefd829f`, with the lane's
existing `node_modules` linked in. Command:

```text
cd /tmp/task-5084b-after/apps/osl-hub-ui
npx vite build
```

Result: exit `0`; Vite printed `✓ 167 modules transformed.` and
`dist/assets/main-DAvOpoaf.js 506.56 kB`.

The package-level `npm run build` was also attempted, but its initial `tsc
--noEmit` phase is red on unrelated pre-existing test/API diagnostics (for
example missing `parseOslChatBurnResult`, `sendTestAppActivity`, and many
onboarding test exports). The task's production build target is the Vite build
above, which passed.

For the required throwaway deletion, I copied that after-carve tree to
`/tmp/task-5084b-break`, deleted only
`src/privacy-settings-section.ts`, and ran the same `npx vite build`.
It failed with exit `1` and the exact diagnostic:

```text
Could not resolve "./privacy-settings-section" from "src/main.ts"
```

The real lane file was not deleted.

## Finish line

- [x] Before and after uidiff tables were actually run; all seven raw/structure
  pairs match to a tenth (`6.6/13.4`, `4.9/7.7`, `4.8/7.4`, `3.0/3.2`,
  `4.4/5.3`, `4.6/4.8`, `3.9/3.6`).
- [x] After-carve production build passed: `167 modules transformed`, exit `0`.
- [x] Deleted-file throwaway copy failed to build: exit `1`, missing file
  `./privacy-settings-section` named by the bundler.
