# Task 4336 evidence — NOT MET (owner hold)

Date: 2026-08-09

Task 4336 remains unticked. No Rust source was edited and no Cargo command was
run, per the task hand-off constraint.

## Owner ruling

Command:

```sh
sed -n '121,130p' /home/liamw/osl-plan/OSL-AUDITS/DECISIONS.txt
```

Output:

```text
R4 · OSL MAIL IS ON HOLD
  Liam: "osl mail is on hold until the other stuff. the other stuff, just put it in the plan."
  NOT cancelled. All 72 tasks in todo/15-mailbox-reading.txt stay as written, marked
  on hold at the top of that file.
  Release the hold when Telegram, Signal and WhatsApp can receive (4090-4103) and the
  wake-up pipe is real (4405).
  The "other stuff" was already in the plan -- verified, no new tasks needed:
    4090-4103  Telegram, Signal, WhatsApp message readers, built from nothing
    4405       the pipe the wake-up channel needs (the library is not even in the project)
    4407       the two real plugs fetch-on-arrival needs; today it only has test ones
```

## Required body-reader gate is also open

Command:

```sh
rg -n -A 10 '^TASK (4336|4359)' /home/liamw/osl-plan/OSL-AUDITS/todo/15-mailbox-reading.txt
```

Output:

```text
366:TASK 4336 - read a Gmail inbox for arrived cover messages
367-gates: 3047, 4120, 4334, 4359
...
783:TASK 4359 - build the part that reads the words of a mail message
784-gates: 4120, 4358
...
790-do: Land this before 4336.
```

Neither task header has `[x]`, and there is no `evidence/4359.md` under the
plan directory.

## Current Gmail state is fixture-only and no carrier is wired

Command:

```sh
rg -n -C 3 'read_gmail_shared_mailbox_labels|open_gmail_shared_mailbox_message' \
  apps/osl-hub/src/service_connections.rs apps/osl-hub/tests/task_3047_gmail_mailbox_reader.rs
```

Output (excerpt):

```text
apps/osl-hub/src/service_connections.rs:342:pub fn read_gmail_shared_mailbox_labels(
apps/osl-hub/src/service_connections.rs:376:pub fn open_gmail_shared_mailbox_message(
apps/osl-hub/tests/task_3047_gmail_mailbox_reader.rs:66:fn task_3047_gmail_reader_lists_labels_sent_messages_and_opens_marked_message() {
apps/osl-hub/tests/task_3047_gmail_mailbox_reader.rs:67:    let mailbox = seeded_gmail_test_mailbox();
```

Command:

```sh
rg -n -C 4 'email_web\(Surface::Gmail|carrier: CarrierEvidence::NotBuilt|delivery: DeliveryEvidence::NotDeliverable' apps/osl-hub/src/claim_state.rs
```

Output (excerpt):

```text
456:    email_web(Surface::Gmail, "Gmail"),
494:/// a tenth provider cannot be added with a quietly different shape.
495:const fn email_web(surface: Surface, _display: &'static str) -> SurfaceClaim {
496-    SurfaceClaim {
497-        surface,
498:        carrier: CarrierEvidence::NotBuilt,
499:        delivery: DeliveryEvidence::NotDeliverable,
500-        blockers: &[ClaimBlocker::WebSurfaceLegalReview],
501-        matrix: MatrixPosition::NoRow,
502-        authority: "PLAN.md r4-5 web-surface legal review; no adapter module exists",
503-        reason: "No carrier is wired for this mail provider. OSL can open it in a browser and it does not protect anything sent through it, and the legal review that would allow one has not concluded.",
```

## Result

**Not met.** R4 puts every mailbox-reading task on hold until 4090–4103 and
4405 complete. Independently, 4359 is still unticked. The existing Gmail code
is a seeded fixture reader and the current claim authority says Gmail has no
wired carrier. No real-mail implementation, test, or completion mark was
created.

### Note on the legacy 4334 checker

`node scripts/task-4334-outside-mail-state.mjs` currently exits `1` before it
prints the Gmail row because an unrelated source expectation for the Outlook
web refusal no longer matches the modified worktree. That failure is not
evidence that Gmail is built and was not changed by this task.
