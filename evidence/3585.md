# Task 3585 evidence — NOT MET

Date: 2026-08-09

Task 3585 remains unticked. No Rust source was edited and no Cargo command was
run, per the task hand-off constraint.

## Blocking prerequisites

Command:

```sh
rg -n -A 9 '^TASK 3425' /home/liamw/osl-plan/OSL-AUDITS/todo/11-typing-into-other-apps.txt
```

Output:

```text
300:TASK 3425 - point every app's placing work at the shared job
301-gates: 0904, 1003, 1033, 1066, 1103, 1133, 1168, 1207, 3406, 3424
302-build: test
303-who: agent
304-tier: normal
305-effort: high
306-run by: codex sol
307-do: Discord, Telegram, Signal, WhatsApp, X, Instagram, Messenger and the email services must all call 3406. None of them may keep its own way of putting text in a box.
308-done when: a search of the code finds exactly 1 place that asks for the front window and exactly 1 place that pastes, and the count of app-specific placing routines is 0.
```

The task header has no `[x]`, and its evidence file is absent:

```sh
for evidence_path in /home/liamw/osl-plan/OSL-AUDITS/evidence/3231.md /home/liamw/osl-plan/OSL-AUDITS/evidence/3425.md; do
  if test -f "$evidence_path"; then printf 'FOUND %s\n' "$evidence_path"; else printf 'MISSING %s\n' "$evidence_path"; fi
done
```

Output:

```text
MISSING /home/liamw/osl-plan/OSL-AUDITS/evidence/3231.md
MISSING /home/liamw/osl-plan/OSL-AUDITS/evidence/3425.md
```

## No existing 3585 implementation

Command:

```sh
rg -n -i 'task[_ -]?3585|TASK3585|placement[_ -]?step.*provider|provider.*placement[_ -]?step' apps/osl-hub apps/osl-hub-ui scripts
```

Exit status: `1`

Output:

```text
(no matches)
```

## Carrier safety boundary

Command:

```sh
sed -n '233,241p' /home/liamw/osl-plan/OSL-AUDITS/proposals/spec-vs-built.md
```

Output:

```text
1. **A proven end-to-end carrier message.** No external carrier has a recorded
   two-person send, recipient decode, and delivery proof. Discord’s own claim
   authority says that run has never completed (apps/osl-hub/src/claim_state.rs:373).
2. **All nine specified email carriers can send protected mail.** The runtime
   records every web-email carrier as NotBuilt/NotDeliverable with no wired
   carrier (apps/osl-hub/src/claim_state.rs:493-504); Outlook desktop is the
   same (claim_state.rs:438-449).
```

## Result

**Not met.** The required shared placement path is not complete (3425 is
unticked), no 3585 fixture/implementation exists, and no carrier has an
end-to-end send path that could honestly provide the requested provider-closure
result. Creating an isolated fixture would not build on the required placement
path and would falsely imply carrier support.
