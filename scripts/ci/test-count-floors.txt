# Per-suite minimum test counts. A suite running FEWER tests than its floor
# fails CI, because the count can only drop when something stopped being
# compiled or collected. See scripts/ci/assert-test-counts.sh.
#
# PROVENANCE MATTERS. Every floor here must be a number somebody measured and
# can point at. An unverified floor is worse than none: it looks like
# enforcement and enforces nothing.
#
# STATUS 2026-07-27: only `osl-hub-core` is actually asserted anywhere
# (rust-test.yml). The rest are declared and NOT wired, so they currently
# enforce nothing. Do not read them as protection.
#
# MEASURE ON A CLEAN TREE. webview's count is 2 in a working tree that has
# been built and 1 in a clean checkout, because vitest collects the compiled
# dist/index.test.js as well as src/index.test.ts. Raising that floor to the
# dirty-tree number would fail every clean CI run, permanently. All values
# below were taken with build output absent.

osl-hub-core=237        # measured, Windows CI, --features core (NOT core,discord-qa-shell)
keyserver-cf=313        # measured 2026-07-27; `npm test` runs TWO vitest invocations,
                        # 310 + 3. The previous floor of 3 was read from the tail of the
                        # output and missed the first invocation entirely - off by ~100x,
                        # so a drop from 313 to 4 would have passed.
cipher-store-cf=18      # measured 2026-07-27, matches
osl-hub-ui=379          # measured 2026-07-27, matches; not wired
webview=1               # measured 2026-07-27 on a CLEAN tree. Do NOT raise to 2:
                        # that is src + the compiled dist copy. Not wired.
keyserver-legacy=72     # measured 2026-07-27, matches; not wired
