{
  "name": "discord-privacy-keyserver",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Key server for discord-privacy-client. Register / fetch identity public keys, upload / fetch wrapped key blobs. Closed-beta deployable: pre-shared admin token gates state-mutating routes; user-id allowlist gates registration; rate limiting on mutations. Plain HTTP at the application layer (TLS provided by hosting platform).",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test 'test/**/*.test.js'"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.3.0",
    "better-sqlite3": "^11.3.0",
    "fastify": "^5.8.5"
  }
}
